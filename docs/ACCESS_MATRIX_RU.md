# API Access Matrix - Restful Booker Platform

## 📋 Обзор аутентификации и доступа к API

**Механизм аутентификации**: Токен передаётся в HTTP Cookie с именем `token`  
**Получение токена**: `POST /auth/login` с телом `{"username": "admin", "password": "password"}`

---

## 📊 Матрица доступа по сервисам

### 1. 🔑 Сервис аутентификации (Auth Service:3004)

| Метод | Endpoint | Аутентификация | Описание |
|-------|----------|----------------|----------|
| POST | `/auth/login` | ❌ Не требуется | Получение токена аутентификации |
| POST | `/auth/validate` | ✅ Требуется | Валидация токена (cookie: `token`) |
| POST | `/auth/logout` | ✅ Требуется | Очистка токена (cookie: `token`) |

### 2. 🏨 Сервис бронирований (Booking Service:3000)

| Метод | Endpoint | Аутентификация | Описание |
|-------|----------|----------------|----------|
| POST | `/booking/` | ❌ Не требуется | Создание нового бронирования |
| GET | `/booking/unavailable` | ❌ Не требуется | Проверка доступности номеров |
| GET | `/booking/summary` | ❌ Не требуется | Сводка по бронированиям |
| GET | `/booking/{id}` | ⚠️ Опционально | Получение бронирования по ID |
| PUT | `/booking/{id}` | ⚠️ Опционально | Обновление бронирования |
| DELETE | `/booking/{id}` | ⚠️ Опционально | Удаление бронирования |
| GET | `/booking/` | ⚠️ Опционально | Список всех бронирований |

### 3. 🛏️ Сервис номеров (Room Service:3001)

| Метод | Endpoint | Аутентификация | Описание |
|-------|----------|----------------|----------|
| GET | `/room/{id}` | ❌ Не требуется | Получение информации о номере |
| GET | `/room/` | ❌ Не требуется | Список номеров (с фильтрацией) |
| POST | `/room/` | ⚠️ Опционально | Создание нового номера |
| PUT | `/room/{id}` | ⚠️ Опционально | Обновление номера |
| DELETE | `/room/{id}` | ⚠️ Опционально | Удаление номера |

### 4. 📨 Сервис сообщений (Message Service:3006)

| Метод | Endpoint | Аутентификация | Описание |
|-------|----------|----------------|----------|
| GET | `/message/` | ❌ Не требуется | Список сообщений (краткий формат) |
| POST | `/message/` | ❌ Не требуется | Создание нового сообщения |
| GET | `/message/{id}` | ❌ Не требуется | Полное сообщение по ID |
| GET | `/message/count` | ❌ Не требуется | Количество сообщений |
| PUT | `/message/{id}/read` | ⚠️ Опционально | Отметить как прочитанное |
| DELETE | `/message/{id}` | ⚠️ Опционально | Удаление сообщения |

### 5. 🏢 Сервис брендирования (Branding Service:3002)

| Метод | Endpoint | Аутентификация | Описание |
|-------|----------|----------------|----------|
| GET | `/branding/` | ❌ Не требуется | Получение информации о бренде |
| PUT | `/branding/` | ⚠️ Опционально | Обновление информации о бренде |

### 6. 📊 Сервис отчётов (Report Service:3005)

| Метод | Endpoint | Аутентификация | Описание |
|-------|----------|----------------|----------|
| GET | `/report/room/{id}` | ❌ Не требуется | Отчёт по конкретному номеру |
| GET | `/report/` | ✅ Требуется | Полный отчёт по всем номерам |

---

## 📝 Легенда статусов аутентификации

| Статус | Значение |
|--------|----------|
| ❌ Не требуется | Метод доступен без токена |
| ✅ Требуется | Токен обязателен (cookie: `token`) |
| ⚠️ Опционально | Токен улучшает функционал или требуется для изменений |

---

**Версия**: 1.0.0  
**Соответствует**: Restful Booker Platform v2.1  
**Обновлено**: 2024-12-24